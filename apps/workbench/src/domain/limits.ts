export const SMOJI_MAX_PACKS = 64
export const SMOJI_MAX_ITEMS_PER_PACK = 600
export const SMOJI_MAX_ITEMS = 6000
export const SMOJI_MANIFEST_MAX_BYTES = 1024 * 1024
/** Own backup bundles embed absolute item srcs, so they are larger than the compact manifest budget. */
export const SMOJI_GROUP_BUNDLE_MAX_BYTES = 4 * 1024 * 1024
export const GRID_RENDER_CHUNK = 72
export const HISTORY_CAPACITY = 50
